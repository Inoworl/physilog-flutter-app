name: PR Tests

on:
  pull_request:
    branches: [dev, main]

jobs:
  analyze:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5.0.1

      - name: Setup Flutter
        uses: ./.github/actions/setup

      - name: Restore Firebase options
        env:
          DEV_FIREBASE_OPTIONS_DART_BASE64: ${{ secrets.DEV_FIREBASE_OPTIONS_DART_BASE64 }}
        run: |
          set -euo pipefail
          if [ -z "$DEV_FIREBASE_OPTIONS_DART_BASE64" ]; then
            echo "::error ::Missing required secret: DEV_FIREBASE_OPTIONS_DART_BASE64"
            exit 1
          fi
          echo "$DEV_FIREBASE_OPTIONS_DART_BASE64" | base64 --decode > lib/firebase_options.dart

      - name: Check formatting
        run: git ls-files '*.dart' ':!:lib/firebase_options*.dart' | xargs dart format --output=none --set-exit-if-changed

      - name: Analyze project source
        run: |
          flutter pub run build_runner build --delete-conflicting-outputs
          flutter analyze --no-fatal-infos

  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: analyze

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5.0.1

      - name: Setup Flutter
        uses: ./.github/actions/setup

      - name: Restore Firebase options
        env:
          DEV_FIREBASE_OPTIONS_DART_BASE64: ${{ secrets.DEV_FIREBASE_OPTIONS_DART_BASE64 }}
        run: |
          set -euo pipefail
          if [ -z "$DEV_FIREBASE_OPTIONS_DART_BASE64" ]; then
            echo "::error ::Missing required secret: DEV_FIREBASE_OPTIONS_DART_BASE64"
            exit 1
          fi
          echo "$DEV_FIREBASE_OPTIONS_DART_BASE64" | base64 --decode > lib/firebase_options.dart

      - name: Generate code
        run: flutter pub run build_runner build --delete-conflicting-outputs

      - name: Run tests
        run: flutter test --dart-define=FLUTTER_TEST=true

  ci_success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [analyze, test]
    if: always()

    steps:
      - name: Check results
        run: |
          if [[ "${{ needs.analyze.result }}" == "success" && "${{ needs.test.result }}" == "success" ]]; then
            echo "All CI checks passed"
          else
            echo "CI checks failed"
            echo "Analyze: ${{ needs.analyze.result }}"
            echo "Test: ${{ needs.test.result }}"
            exit 1
          fi
